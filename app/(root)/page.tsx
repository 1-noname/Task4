import { HomePage } from "@/pages/home";

import { Metadata } from "next";

export const metadata: Metadata = {
  title: "MovieShop | Home",
};

export default function Page() {
  return <HomePage />;
}
